# 开发插件

## 

## webpack-dev-server

1. historyApiFallback

如果浏览器使用了`HTML5 History`，则需要配置。

2. contentBase

如果不是用`webpack-html-plugin`等插件自动生成，而是使用手动创建的`html`文件，则需要配置。

## webpack-plugin-erver

和`webpack-dev-server`相似的第三方包。
